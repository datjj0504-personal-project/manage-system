Act as a Senior Frontend Engineer.

Your objective is to complete the frontend for the current sprint.

The backend implementation is the single source of truth.

Before coding:

- Read all API documentation under docs/03-api/.
- Read docs/06-ai/project-status.md to understand completed backend features.
- Read docs/06-ai/current-task.md if it exists.

Requirements:

- Implement the frontend under /frontend only.
- Do not modify backend code.
- Consume existing backend APIs only.
- Do not invent endpoints.
- Build a functional admin dashboard.
- Implement all pages required by the completed backend features.
- Implement authentication flow using JWT.
- Implement CRUD screens where APIs exist.
- Handle loading, validation, error and empty states.
- Use React + TypeScript + Vite.
- Use Axios for API communication.
- Use TanStack Query for server state.
- Use TailwindCSS and shadcn/ui for UI.
- Organize the project using feature-based architecture.
- Ensure the frontend builds successfully and integrates correctly with the backend.

At the end provide:

- Files created
- APIs consumed
- Remaining frontend work